<%= form_for(@product) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :shopify_product_id %><br>
    <%= f.number_field :shopify_product_id %>
  </div>
  <div class="field">
    <%= f.label :last_shopify_sync %><br>
    <%= f.datetime_select :last_shopify_sync %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
